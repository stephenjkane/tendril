package com.tendril.interview;

/**
 * A service used to calculate the mean, median, range, and standard deviation.
 *
 */
public interface MathService {

    /**
     * Generates the mean value of the data
     * @return
     */
    public double mean(int[] data);

    /**
     * Generates the
     */
}
